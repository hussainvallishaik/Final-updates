{
  "name": "auto-update",
  "version": "1.0.0",
  "description": "just test purpose",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always"


  },
  "repository": {
    "type" : "git",
    "url" : "https://github.com/hussainvallishaik/autoupdates.git"
  },
  "author": "botpathTeam",
  "license": "ISC",
  "devDependencies": {
    "electron": "^9.4.0",
    "electron-builder": "^22.3.2"
  },
  "dependencies": {
    "electron-updater": "^4.2.2"
  }
}
